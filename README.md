# REST API SERVER - USERS AND POSTS (blog)

This is a test server to practice node with typescript and mongo db to generate a simple rest api server.

After clone this project, run

```bash
npm i
```
